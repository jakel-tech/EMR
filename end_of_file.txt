const AboutPage = ({ isRTL }: { isRTL: boolean }) => (
  <div className="p-8 max-w-4xl mx-auto min-h-[60vh] flex flex-col items-center justify-center text-center">
    <div className="bg-primary-50 p-4 rounded-full mb-6">
      <Info size={48} className="text-primary-600" />
    </div>
    <h2 className="text-3xl font-black mb-4 text-gray-900">{isRTL ? "عن منصة سلامات" : "About Salamat Platform"}</h2>
    <p className="text-gray-600 max-w-lg leading-relaxed">
      {isRTL 
        ? "سلامات هو نظام موحد لإدارة المستشفيات والمراكز الطبية، يهدف إلى رقمنة العمليات السريرية والإدارية وتوفير تجربة صحية متكاملة."
        : "Salamat is a unified hospital management system designed to digitize clinical and administrative operations, providing an integrated healthcare experience."}
    </p>
    <div className="mt-8 pt-8 border-t w-full max-w-xs text-sm text-gray-400 font-medium uppercase tracking-widest">
      Version 2026.06.23
    </div>
  </div>
);

const GlobalSearchModal = ({ 
  isOpen, 
  onClose, 
  isRTL, 
  currentUser, 
  patients, 
  hospitals, 
  users, 
  appointments, 
  onNavigate 
}: any) => {
  const [query, setQuery] = React.useState("");
  if (!isOpen) return null;

  return (
    <div className="fixed inset-0 z-[100] flex items-center justify-center bg-black/60 backdrop-blur-md p-4">
      <motion.div 
        initial={{ opacity: 0, scale: 0.9, y: 20 }}
        animate={{ opacity: 1, scale: 1, y: 0 }}
        exit={{ opacity: 0, scale: 0.9, y: 20 }}
        className="bg-white dark:bg-gray-900 rounded-[2rem] w-full max-w-2xl overflow-hidden shadow-2xl border border-white/20"
      >
        <div className="p-6 border-b dark:border-gray-800 flex items-center gap-4 bg-gray-50/50 dark:bg-gray-800/50">
          <div className="bg-white dark:bg-gray-700 p-2 rounded-xl shadow-sm">
            <Search className="text-primary-500" size={24} />
          </div>
          <input 
            autoFocus
            value={query}
            onChange={(e) => setQuery(e.target.value)}
            className="flex-1 bg-transparent border-none outline-none text-xl font-medium placeholder:text-gray-400 text-gray-900 dark:text-white"
            placeholder={isRTL ? "البحث في سلامات..." : "Search in Salamat..."}
          />
          <button 
            onClick={onClose} 
            className="p-2 hover:bg-gray-200 dark:hover:bg-gray-700 rounded-full transition-colors text-gray-500"
          >
            <X size={24} />
          </button>
        </div>
        
        <div className="p-8 min-h-[300px] flex flex-col items-center justify-center">
          <div className="bg-gray-100 dark:bg-gray-800 p-6 rounded-full mb-4">
            <Search size={48} className="text-gray-300 dark:text-gray-600" />
          </div>
          <h3 className="text-lg font-bold text-gray-400 mb-2">
            {query 
              ? (isRTL ? "لا توجد نتائج لـ " : "No results for ") + `"${query}"`
              : (isRTL ? "ابدأ الكتابة للبحث..." : "Search in Salamat...")
            }
          </h3>
          <p className="text-sm text-gray-400 text-center max-w-xs">
            {isRTL 
              ? "يمكنك البحث عن المرضى بالاسم أو الرقم الوطني، أو البحث في المستشفيات والموظفين."
              : "Search for patients by name or ID, or find hospitals and staff members."}
          </p>
        </div>
      </motion.div>
    </div>
  );
};

export default App;�
