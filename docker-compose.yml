services:
  cmms-app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    volumes:
      - cmms_data:/app/data
    restart: unless-stopped

volumes:
  cmms_data:
